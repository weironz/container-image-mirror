quay.io:
    images-by-tag-regex:
        ceph/ceph-grafana: ^v([0-9]+\.){0,2}(\*|[0-9]+)$
        prometheus/prometheus: ^v([0-9]+\.){0,2}(\*|[0-9]+)$
        prometheus/node-exporter: ^v([0-9]+\.){0,2}(\*|[0-9]+)$
        prometheus/alertmanager: ^v([0-9]+\.){0,2}(\*|[0-9]+)$
quay.ceph.io:
    images-by-tag-regex:
        ceph-ci/ceph: master
        ceph-ci/ceph: pacific
        ceph-ci/ceph: quincy
        ceph-ci/ceph: octopus
