docker.io:
    images-by-tag-regex:
        calico/cni: ^v([0-9]+\.){0,2}(\*|[0-9]+)$
        calico/node: ^v([0-9]+\.){0,2}(\*|[0-9]+)$
        calico/kube-controllers: ^v([0-9]+\.){0,2}(\*|[0-9]+)$
    credentials:
        username: $USERNAME
        password: $PASSWORD
